Bootstrap: docker
From: python:3.12-slim
%files
    . /opt/pcpi
%post
    cd /opt/pcpi
    python -m pip install --upgrade pip
    python -m pip install -e ".[dev]"
%test
    cd /opt/pcpi
    python -m pytest -q
%runscript
    exec pcpi-candidate-certification "$@"
